const fun=()=>({user:"vj"});
console.log(fun());


const one= ()=>{
    let user="vj";
    console.log(user);
    // console.log(this.user);//undefined
    console.log(this);
}
one();

const person={
    name:"vj",
    greet:function(){setTimeout(function(){console.log(this.name);},3000); }
};
person.greet();

const person = {
    name: "vj",
    greet: function() {
        setTimeout(function() {
            console.log(this.name); // "vj"
        }.bind(this), 3000);
    }
};
person.greet();
